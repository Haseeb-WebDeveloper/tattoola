import { Text, View } from 'react-native';

export default function InboxScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-red-500">
      <Text className="text-xl font-bold text-primary-foreground">Inbox</Text>
    </View>
  );
}
