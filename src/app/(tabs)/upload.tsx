import { Text, View } from 'react-native';

export default function UploadScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-primary">
      <Text className="text-xl font-bold text-primary-foreground">Upload</Text>
    </View>
  );
}
